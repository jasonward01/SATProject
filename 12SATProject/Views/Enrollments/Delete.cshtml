@model SATProject.DATA.EF.Enrollment

@{
    ViewBag.Title = "Delete Enrollment";
}
<div class="text-center mb-3 mt-3">
    <h3>Are you sure you want to delete this record?</h3>
</div>

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EnrollmentDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EnrollmentDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(modelItem => Model.ScheduledClass.Course.CourseName)
        </dt>

        <dd>
            @Html.DisplayFor(Model => Model.ScheduledClass.Course.CourseName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ScheduledClass.InstructorName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ScheduledClass.InstructorName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Student.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Student.FullName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-warning mt-3 mb-3" /> |
            @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-danger"})
        </div>
    }
</div>
